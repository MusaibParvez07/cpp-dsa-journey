#include <iostream>
using namespace std;

int main()
{
    cout << "Hello and Welcome to C++ World!" << endl;
    return 0;
}
/*iostream--It is a Header file library that allows to work with input and output objects.
Namespace std--It means we can use names for objects and variables from the Standard Library.
Here int is a keyword that means return something and sometimes void is also used when nothing
needs to be returned.Main is a function and everything inside it is executed.
Cout <<--is like printf and cin >> is like scanf of c language.*/